name: touch_me
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  table_calendar: ^3.0.9

  google_maps_flutter: ^2.7.0
  geolocator: ^12.0.0
  permission_handler: ^11.3.1

  geocoding: ^3.0.0
  url_launcher: ^6.3.0
  flutter_rating_bar: ^4.0.1
  # Removed duplicate dependencies key and its content
  



  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/offers/
    - assets/services/
    - assets/saloons/
    - assets/favourites/
